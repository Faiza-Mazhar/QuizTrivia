<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="text_black" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#03040B</item>
        <item name ="android:textAlignment">center</item>
        <item name ="android:textStyle">bold</item>
    </style>

    <style name="lighterGreenWithBlackText" parent="text_black">
        <item name="android:background">@color/lighterGreen</item>
        <item name="android:elevation">10dp</item>
        <item name="android:radius">10dp</item>
        <item name="android:paddingHorizontal">8dp</item>
        <item name="android:paddingVertical">8dp</item>
    </style>

    <style name="fragmentBackground">
        <item name="android:background">@color/moreLightestGreen</item>
    </style>
</resources>